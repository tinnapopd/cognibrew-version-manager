syntax = "proto3";

package cognibrew.vectordb;

message PersonUpdate {
  string         username   = 1;  // display name / label
  repeated float embedding  = 2;  // face embedding vector (e.g. 512-dim)
}
