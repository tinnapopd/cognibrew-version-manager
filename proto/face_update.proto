syntax = "proto3";

package cognibrew.vectordb;

message PersonUpdate {
  string         face_id    = 1;  // unique face id
  string         username   = 2;  // display name / label
  repeated float embedding  = 3;  // face embedding vector (e.g. 512-dim)
}
